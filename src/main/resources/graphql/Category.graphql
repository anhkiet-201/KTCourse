type Category {

    categoryId: String

    name: String

    descrition: String
}

extend type Query {
    getKTCourseCategories: [Category]!
    getCourseByCategoryId(categoryId: String): Pagination!
}